# Core tooling and automation
requests
pre-commit

# Backend (FastAPI, DB, tasks, etc.)
fastapi
uvicorn
sqlalchemy
alembic
psycopg2-binary
python-dotenv
pydantic
celery
redis

# Frontend (for completeness, but handled by npm/yarn)
# (No Python packages needed for React)

# Testing
pytest
pytest-cov
httpx
pytest-xdist
pexpect

# Linting/Formatting
black
flake8
isort

# Type checking
mypy

# Optional: for S3/MinIO integration
boto3

# Optional: for security
python-jose
passlib[bcrypt]

# Optional: for code quality
bandit

# Mutation testing tools
mutmut
cosmic-ray

#property-based testing
hypothesis

#auto-generating tests documentation from code/comments
pytest-clarity
pytest-sphinx

# Add more as needed for your stack
